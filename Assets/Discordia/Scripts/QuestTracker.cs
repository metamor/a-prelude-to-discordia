using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// quest tracker or quest manager? keeps track of the current quests and any quests that the player had completed?

public class QuestTracker : MonoBehaviour
{
    // list of Quest acceptedQuests
    // list of Quest completedQuests


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
