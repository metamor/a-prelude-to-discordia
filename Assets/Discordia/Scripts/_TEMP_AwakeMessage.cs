using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class _TEMP_AwakeMessage : MonoBehaviour
{
    private void Awake()
    {
        Debug.Log("Temp Awake Message");
    }

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Temp Start Message");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Temp Update Message");
    }
}
