using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Animations;

// component on ui that will be in charge of changing the color of everything like background
// uiLayout object that it save that has fields such as background color, etc?

public class UIController : MonoBehaviour
{
    /*private void Awake()
    {
    }*/

    // Start is called before the first frame update
    void Start()
    {
        // add itself to gamemanager ui
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
