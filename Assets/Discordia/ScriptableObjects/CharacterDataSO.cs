using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterDataSO : DataSO
{
    // enemies with the same name but different sprite will be a new SO; male raider and female raider are 2 SO as each char might have different voices and lines


    // item array of the items the enemy has; some items can drop while others can only be stolen

    // [Header("Weapons")]? use of header

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
